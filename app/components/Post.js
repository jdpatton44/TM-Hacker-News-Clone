import React from 'react'

export default class Post extends React.Component {
    render() {
        return (
            <Title title={title} />
            {kids.map(kid => {
                return (
                    <p>{kid}</p>)}
        ) 
    }
}